<!DOCTYPE html>
<html lang="it">
    <%- include('partials/head') %>
<body class="bg-light">

    <%- include('partials/navbar') %>

    <div class="container mt-5">
        <h1>Riepilogo Ordine</h1>
        <h3>Per modificare l'ordine torna indietro al <a href="/cart">carrello</a></h3>
        <div class="row">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">Prodotti</h5>
                        <ul class="list-group">  
                        <% myOrder.forEach(articolo => { %>
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            <div>
                                <%= articolo.codart %> (<%= articolo.price %> €/unità)
                                <span class="badge bg-primary rounded-pill ms-2"><%= articolo.qty %></span>
                            </div>
                            <span><%= (articolo.qty * articolo.price).toFixed(2) %> €</span>
                        </li>
                        <% }); %>
                    </div>
                </div>
                <div class="card mt-3">
                    <div class="card-body">
                        <h5 class="card-title">Indirizzo di Spedizione</h5>
                        <p>Nome: Mario Rossi</p>
                        <p>Indirizzo: Via Roma, 1</p>
                        <p>Città: Milano</p>
                    </div>
                </div>
            </div>
            <div class="col-md-4">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">Totale Ordine</h5>
                        <p>Subtotale: <%= subtotal %> €</p>
                        <p>Spedizione: <%= shippingCost %> €</p>
                        <p>Totale: <%= total %> €</p>
                        <form action="/checkout" method="post">
                            <input type="submit" value="Proceed to Checkout">
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
</body>
</html>